# Runtime dependencies for the offline / generator paths.
# POX itself is not pip-installable; clone it separately from
#   https://github.com/noxrepo/pox
# and run the controller with: ./pox.py log.level --DEBUG ddos_sdn.detector.pox_controller

# Packet crafting + replay
scapy>=2.5

# Cross-platform network interface discovery (replaces ifconfig|awk)
psutil>=5.9

# YAML config loading (Phase 1)
PyYAML>=6.0

# Test runner (Phase 2)
pytest>=8.0

# CICDDoS2019 sample extraction (Phase 2 — scripts/extract_sample.py)
numpy>=1.26
pandas>=2.2

# ML stack (Phase 3 — consumed by notebooks/train_pca_and_rf.py, PCADetector,
# MLDetector). The exact versions below are the versions used to produce the
# committed models/*.joblib artifacts; reloading them on a different minor
# scikit-learn version may emit InconsistentVersionWarning but unpickling
# remains functional. If you regenerate the artifacts, pin to whatever sklearn
# you run.
scikit-learn==1.8.0
joblib==1.5.3

# Streamlit dashboard (Phase 4a — consumed by dashboard.py and the
# Streamlit Community Cloud deploy via streamlit_app.py).
streamlit>=1.40
plotly>=5.20
# watchdog enables the --mode tail path for production telemetry replay.
watchdog>=4.0
# Make this project itself importable on Streamlit Cloud
# (locally, devs use `pip install -e .` once; Cloud reinstalls on every deploy)
-e .